# Calls the reusable Go CI in lib-common. See KPD-53.
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  go:
    uses: niaga-labs/niaga-labs-pet-lib-common/.github/workflows/go-ci.yml@main
    with:
      needs-lib-common: false
      needs-lib-proto: false
